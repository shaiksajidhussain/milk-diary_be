/**
 * Shared metadata for OpenAPI / Swagger UI integration later.
 * See `docs/openapi.yaml` for the full contract.
 */
export const openApiInfo = {
  title: 'Dairy Milk Collection Management API',
  version: '1.0.0',
  description: 'REST API for admin auth, farmers, milk collections, and dashboard stats.',
}
